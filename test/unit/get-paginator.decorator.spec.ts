import { getTestData } from "../test-data/test-data";

describe("Model module: OrgConfigController", () => {
    const data = getTestData();

    beforeEach(async () => {});

    it("should be defined", () => {
        expect(data).toBeDefined();
    });
});
